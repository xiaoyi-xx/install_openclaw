

# OpenClaw 安装和配置工具

本项目提供了一套完整的 OpenClaw 安装、启动和配置工具，支持 Windows 平台的自动化部署和图形化配置管理。

## 目录结构

```
install/
├── config_gui.py         # OpenClaw 配置修改工具（Python GUI）
├── install_openclaw.ps1  # OpenClaw 安装脚本（PowerShell）
└── start_openclaw.bat   # OpenClaw 启动脚本（批处理）
```

## 快速开始

### 1. 安装 OpenClaw

运行 PowerShell 安装脚本，自动完成所有依赖项的安装：

```powershell
# 右键点击 install_openclaw.ps1，选择"以PowerShell运行"
```

安装脚本会自动完成以下操作：
- 切换到用户目录
- 检测并安装 Node.js（如未安装）
- 检测并安装 Git（如未安装）
- 安装 OpenClaw 最新版本
- 运行新手引导并配置服务

### 2. 启动 OpenClaw

双击运行启动脚本：

```bash
start_openclaw.bat
```

脚本会自动：
- 检查 OpenClaw 安装状态
- 执行登录操作
- 启动网关（端口 18789）
- 自动打开浏览器访问网关

### 3. 配置管理
注意：这个管理器可能只适用我自己的配置文件，最优选择是使用官方的配置脚本进行配置
使用图形界面工具修改配置：

```bash
python config_gui.py
```

## 文件说明

### install_openclaw.ps1

PowerShell 自动化安装脚本，负责部署完整的 OpenClaw 环境。

**主要功能：**
- Node.js 自动检测与安装
- Git 自动检测与安装
- OpenClaw 最新版安装
- 新手引导自动执行
- 服务安装与配置
- 完善的错误处理机制

**使用前提：**
- Windows 7 或更高版本
- 管理员权限（推荐）
- 稳定的网络连接

### start_openclaw.bat

批处理启动脚本，用于快速启动 OpenClaw 网关服务。

**主要功能：**
- 安装状态检查
- 自动登录验证
- 网关启动（端口 18789）
- 浏览器自动打开
- 便捷的停止机制

**使用说明：**
双击运行后，等待网关启动完成，浏览器会自动打开。操作完成后按任意键停止服务。

### config_gui.py

Python 图形界面配置管理工具，提供可视化的配置修改功能。

**主要功能：**
- 图形化配置界面
- 多种配置类型管理
- API 模型列表获取
- 配置文件自动备份
- 实时配置预览

**配置选项：**

| 分类 | 包含内容 |
|------|----------|
| 元数据 | 版本号、最后修改时间 |
| 模型配置 | 模型模式、提供者名称、baseUrl、apiKey、模型ID |
| 代理配置 | 主模型、工作目录、最大并发数 |
| 网关配置 | 端口、模式、绑定地址、认证令牌 |

**依赖项：**
- Python 3.x
- tkinter（Python 内置）

## 详细安装流程

### 步骤一：运行安装脚本

1. 找到 `install_openclaw.ps1` 文件
2. 右键点击 → 选择"以PowerShell运行"
3. 按照提示完成安装

### 步骤二：新手引导配置

首次安装后会自动运行新手引导，按提示完成：
- 基础配置设置
- 服务启动配置

### 步骤三：启动服务

```bash
# 双击运行
start_openclaw.bat
```

### 步骤四：（可选）修改配置

如需调整配置：

```bash
# 运行配置工具
python config_gui.py
```

## 常见问题

### 安装失败

**可能原因：**
- 网络连接不稳定
- 权限不足
- 依赖项安装失败

**解决方案：**
- 检查网络连接
- 使用管理员权限运行
- 确认 Node.js 和 Git 已正确安装
- 查看错误日志

### 启动失败

**可能原因：**
- OpenClaw 未正确安装
- 环境变量未配置
- 端口被占用

**解决方案：**
- 确认 OpenClaw 已成功安装
- 检查 PATH 环境变量
- 检查端口 18789 占用情况

### 配置问题

**可能原因：**
- 配置文件格式错误
- API 密钥无效
- 模型配置不正确

**解决方案：**
- 使用 `config_gui.py` 检查配置
- 确认 API 密钥有效
- 验证模型参数

## 技术支持

如遇问题，请参考：
- OpenClaw 官方文档
- 社区技术支持渠道

## 许可证

本项目仅提供安装和配置工具，相关许可证请参考 OpenClaw 主项目。